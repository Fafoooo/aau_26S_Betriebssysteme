Linux Questions

1. File Types (7):
- Regular files
d Directories  
l Symbolic links
c Character devices
b Block devices
p Named pipes/FIFOs
s Unix domain sockets

2. Show only month in ls:
ls -la --time-style="+%b" | awk '{print $6, $7}'

3. Create 1GB random file:
dd if=/dev/urandom of=random.bin bs=1M count=1024